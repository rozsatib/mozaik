# sim-test3-neo14-smoke — 1-trial smoke of the NEW container/code end-to-end (mozaik-update).
# De-risks the new stack (neo 0.14.4 / renamed seed / PR#20 params / pyNN warning / new imagen)
# before the full Leg-1/Leg-2 campaign. Default LGN mode (original_2024_lgn_mode:False).
# Writes SelfSustainedPushPull_trial0_chunk0_____lgn_stepcurrentsource_noise_seed:0 (new naming,
# does NOT collide with old ..._____noise_seed:0 datastores). Consumed by cluster/submit.sh.
WORKLOAD=sim

# --- scheduler ---
PARTITION=medium96s
CONSTRAINT=sapphirerapids
NTASKS=12
MEM=250G
TIME=02:00:00
ARRAY=0-0
JOB_NAME=mozaik-neo14-smoke
OUTPUT=./slurm_neo14_smoke/mozaik-neo14-smoke-%A_%a.out
ERROR=./slurm_neo14_smoke/mozaik-neo14-smoke-%A_%a.err

# --- runtime ---
N_CHUNKS=1
CHUNK_DIR=/data/MOZAIK/mozaik_chunk_test3
# NEW container (this is what the smoke validates); absolute path.
SIF_IMAGE=/mnt/vast-nhr/projects/nix00014/goirik/MOZAIK-new/mozaik-sif/mozaik-opt_2026-07-10.sif
